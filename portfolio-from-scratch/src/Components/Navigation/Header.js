import React from "react";
import "./Header.css";
// Might not use a header considering my design choice, maybe just a side bar
const Header = () => {
  return (
    <header>
      <div className="container-fluid">
        
      </div>
    </header>
  );
};

export default Header;
